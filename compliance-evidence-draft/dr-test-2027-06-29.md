# BCP/DR restore/failover test — 2027-06-29 (A1.3/CC7.5/CC9.1)
**Process:** dr-test   **Cadence:** annual   **Review date:** 2026-06-30   **Drafted by:** EA (compliance-drafter)
**Status:** DRAFT — **WORLD-ACT: the executive performs the actual restore test and attaches the artifact-of-performance below. I drafted the record template and evidence structure only.**

## What I gathered

**Policy context:**
- `compliance/policies/business-continuity-dr-policy.md` — last modified 2026-06-29. Defines RTO/RPO as `[e.g. 24h]` placeholders. Requires ≥ annual restore test and the dated record.
- `compliance/control-matrix.md` — C20: "restore test is org-operated."
- `compliance/control-register.md` — dr-test process is due; no prior artifacts exist in the evidence ledger.

**Repository-level DR posture (in-repo scope):**
- **Repository / code:** GitHub is the system of record; recoverable via clone + GitHub's own DR. Full history (incl. `compliance-evidence` branch) distributed across every clone.
- **Compliance evidence:** the `compliance-evidence` branch is git-replicated.
- **Model proxy / spend ledger:** held by the proxy subprocessor — backup/DR is out of this repo's control.
- **Secrets:** re-provisionable; not backed up in plaintext. Maintain a recovery runbook.

**Criteria coverage:**
| Criteria | Statement | Evidence path |
|----------|-----------|--------------|
| A1.3 | Test recovery/continuity procedures periodically | Dated restore-test record (this document + artifact-of-performance) |
| CC7.5 | Recover from identified security incidents | DR test demonstrating successful recovery |
| CC9.1 | Mitigate business-disruption risk (BCP/DR, insurance) | BCP/DR policy + test record + cyber-insurance declaration (external) |

## Assessment — RECORD ONLY (world-act — no test performed yet)

> ⚠️ **No restore test has been executed for this interval.** The record template and evidence structure are prepared below. The executive must perform the recovery test and fill in the **Test results** section and attach the **artifact-of-performance**.

### DR readiness posture (pre-test — assessed from repo state)

| Element | Status | Detail |
|---------|--------|--------|
| BC/DR policy | ✅ On file | `business-continuity-dr-policy.md` — but RTO/RPO are `[e.g. 24h]` placeholders |
| Repository backup | ✅ Git-replicated | Distributed across all clones; GitHub-managed |
| Evidence backup | ✅ Git-replicated | `compliance-evidence` branch in every clone |
| Model proxy DR | ⚠️ External | Per policy: "must be confirmed with the proxy operator" — no confirmation on file |
| Secrets recovery | ⚠️ Runbook needed | Policy says "maintain a recovery runbook" — none visible in repo |
| RTO/RPO defined | ❌ PLACEHOLDER | Policy still has `[e.g. 24h]` — needs RTO/RPO assignment |
| Cyber-insurance | ❌ Not on file | CC9.1 external leg — not in repo scope |

## Findings

- ⚠️ **FLAG — RTO/RPO are placeholders in the BC/DR policy.** Before a valid DR test can be assessed, the RTO (max downtime) and RPO (max data loss) must be set to actual targets. This is the executive's decision.
- ⚠️ **FLAG — No secrets recovery runbook visible in the repo.** Policy requires one. The DR test should validate that secrets can be re-minted from documented recovery procedures.
- ⚠️ **FLAG — Proxy DR status unconfirmed.** The model proxy is a critical dependency (agent runs fail without it) per the BC/DR policy — but its backup/DR posture has not been confirmed with the operator.
- ℹ️ No prior dr-test artifact exists (first interval since install). No baseline to compare.

## Open items needing the executive

- **🔴 Set RTO/RPO in the BC/DR policy** — This is a prerequisite. Edit `compliance/policies/business-continuity-dr-policy.md` to replace `[e.g. 24h]` with your actual RTO (e.g. 24h) and RPO (e.g. 24h) targets. Only then can a restore test be meaningfully evaluated against objectives.
- **🔴 Perform the recovery test and record results below** — See the **Test results** section. Document what was tested, the outcome, and any exceptions found.
- **🔴 Attach the artifact-of-performance** — Commit evidence of the test execution (screenshot, log output, runbook with timestamps) to the repo. This is the auditor-visible proof that the test occurred.
- **Confirm proxy DR posture** — Contact the Open Autonomy proxy operator and confirm their backup/DR arrangements; store the response.
- **Create a secrets recovery runbook** — Document step-by-step instructions for re-minting secrets (e.g. `INFRASTRUCTURE.md` or `SECRETS-ROTATION.md`).
- **Confirm cyber-insurance** (CC9.1 external leg) — The BC/DR policy references insurance as a business-disruption mitigant. If insurance is in place, note the policy number and expiry; otherwise flag for the next risk assessment.

## Coverage / gaps

- ✅ BC/DR policy reviewed — available and last-modified within the year.
- ✅ Repository-level DR architecture documented — from `control-matrix.md` and policy.
- ✅ Required evidence structure prepared for the executive to complete.
- > un-evidenced: **Actual DR restore test** — world-act; the executive performs the test and records results below. I cannot script the test because it involves external infrastructure (org GitHub admin access, model proxy, secrets re-minting).
- > un-evidenced: **RTO/RPO assignment** — executive decision; must set targets in the BC/DR policy.
- > un-evidenced: **Secrets recovery runbook** — executive (or infra lead) must create.
- > un-evidenced: **Proxy DR confirmation** — requires external communication with the proxy operator.
- > un-evidenced: **Cyber-insurance evidence** — CC9.1 external leg; the executive confirms.

---

## 🔲 TEST RESULTS (the executive fills this section)

> **Instructions:** After performing the restore/failover test, describe what was tested and the result. Attach or link the artifact-of-performance below. If there were exceptions, note them and the plan to remediate.

| Test element | Result (✅ / ❌ / ⚠️) | Notes |
|---|---|---|
| Repository clone + full history recovery | | |
| `compliance-evidence` branch integrity | | |
| Workflow re-provisioning (if tested) | | |
| Secrets re-minting (if tested) | | |
| Model proxy reachability (if tested) | | |
| RTO achieved | | |
| RPO achieved | | |

**Narrative description of the test:** (what was done, steps followed, any deviations from the expected procedure)

**Artifact-of-performance:** (commit the test evidence, e.g. `compliance-evidence/dr-test/2027-06-29/` — log output, screenshots, dated runbook)

**Exceptions / corrective actions:**

| Exception | Severity | Remediation plan | Target date |
|-----------|----------|-----------------|-------------|
| | | | |
| | | | |

**Date of test:**
**Tester(s):**